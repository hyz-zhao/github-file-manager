# GitHub文件同步管理器 - 新手超详细教程

## 目录
1. [环境准备](#环境准备)
2. [GitHub仓库创建](#github仓库创建)
3. [GitHub令牌申请](#github令牌申请)
4. [程序运行方法](#程序运行方法)
5. [配置填写说明](#配置填写说明)
6. [功能使用指南](#功能使用指南)
7. [常见问题解答](#常见问题解答)

---

## 环境准备

### 1. 安装Python
如果您的电脑还没有安装Python，请按以下步骤操作：

**Windows系统：**
1. 访问 Python官网：https://www.python.org/downloads/
2. 下载最新的Python 3.x版本（推荐3.8或以上）
3. 运行安装程序，**务必勾选 "Add Python to PATH"**
4. 点击 "Install Now" 完成安装

**验证安装：**
- 按 `Win + R`，输入 `cmd`，按回车打开命令提示符
- 输入 `python --version`，如果显示版本号则安装成功

### 2. 安装PyGitHub库
打开命令提示符（Windows）或终端（Mac/Linux），运行：

```bash
pip install PyGithub
```

如果安装速度慢，可以使用国内镜像：
```bash
pip install PyGithub -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## GitHub仓库创建

### 步骤1：注册GitHub账号
1. 访问 https://github.com
2. 点击右上角 "Sign up" 注册
3. 填写用户名、邮箱、密码
4. 完成邮箱验证

### 步骤2：创建新仓库
1. 登录GitHub后，点击右上角 "+" 号
2. 选择 "New repository"

![创建仓库](https://docs.github.com/assets/cb-39085/mw-1440/images/help/repository/repo-create.webp)

3. 填写仓库信息：
   - **Repository name**: 输入仓库名称（如：`my-files`）
   - **Description**: 可选，填写仓库描述
   - **Public/Private**: 选择 Public（公开）或 Private（私有）
     - 推荐：选择 **Private**（私有），只有您自己能看到文件
   - **勾选 "Add a README file"**
   
4. 点击 "Create repository" 创建仓库

### 步骤3：记录仓库信息
创建成功后，您会看到仓库地址，格式如：
```
https://github.com/您的用户名/仓库名
```

**请记下您的仓库名称**，格式为：`用户名/仓库名`
- 例如：`zhangsan/my-files`

---

## GitHub令牌申请

### 为什么需要令牌？
GitHub令牌（Personal Access Token）相当于一把"钥匙"，让程序能够访问您的GitHub仓库。

### 步骤1：进入设置页面
1. 登录GitHub
2. 点击右上角头像
3. 选择 "Settings"（设置）

### 步骤2：进入开发者设置
1. 在左侧菜单最下方，点击 "Developer settings"

![开发者设置](https://docs.github.com/assets/cb-42178/mw-1440/images/help/settings/developer-settings-menu.webp)

### 步骤3：创建个人访问令牌
1. 点击左侧 "Personal access tokens"
2. 选择 "Tokens (classic)"
3. 点击 "Generate new token"
4. 选择 "Generate new token (classic)"

### 步骤4：配置令牌权限
填写令牌信息：

1. **Note（备注名称）**：输入令牌名称
   - 例如：`文件管理器令牌`

2. **Expiration（有效期）**：选择有效期
   - 推荐：选择 "90 days" 或 "No expiration"（永不过期）

3. **Select scopes（选择权限）**：**必须勾选以下权限**
   - ✅ **repo** - 完整的仓库访问权限（最重要！）
     - ✅ repo:status
     - ✅ repo_deployment
     - ✅ public_repo
     - ✅ repo:invite
     - ✅ security_events
   
   只需要勾选整个 **repo** 即可，会自动勾选所有子选项。

4. 点击底部绿色按钮 "Generate token"

### 步骤5：保存令牌
**⚠️ 非常重要！**
- 令牌只会显示一次，请立即复制保存！
- 令牌格式类似：`ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`
- 如果忘记了，只能重新创建新令牌

![保存令牌](https://docs.github.com/assets/cb-39526/mw-1440/images/help/settings/personal-access-token-page.webp)

---

## 程序运行方法

### 方法1：双击运行（推荐新手）
1. 找到 `file_manager.py` 文件
2. 右键点击文件
3. 选择 "打开方式" → "Python"

如果无法直接运行，请使用方法2。

### 方法2：命令行运行
1. 按 `Win + R`，输入 `cmd`，按回车
2. 使用 `cd` 命令进入程序所在目录
   ```bash
   cd C:\Users\您的用户名\Desktop\wenjian1
   ```
3. 运行程序
   ```bash
   python file_manager.py
   ```

### 方法3：在IDE中运行
如果您使用 PyCharm、VS Code 等IDE：
1. 打开 `file_manager.py` 文件
2. 点击运行按钮（通常是右上角的绿色三角形）

---

## 配置填写说明

### 首次运行配置
程序启动后，您会看到主界面。首次使用需要配置GitHub连接：

1. 点击左侧 "⚙️ 配置GitHub" 按钮
2. 在弹出的对话框中填写：

#### 访问令牌
- 粘贴您之前保存的GitHub令牌
- 格式如：`ghp_xxxxxxxxxxxxxxxxxxxx`
- 输入时会显示为 `****` 保护隐私

#### 仓库名称
- 格式：`您的GitHub用户名/仓库名`
- 例如：`zhangsan/my-files`
- 注意：使用英文斜杠 `/` 分隔

#### 分支名称
- 默认填写：`main`
- 如果您的仓库使用 master 分支，则填写 `master`
- 大多数新仓库默认使用 `main`

3. 点击 "测试连接" 按钮
4. 如果显示 "GitHub连接成功！"，说明配置正确
5. 点击 "保存配置" 保存设置

---

## 功能使用指南

### 1. 上传文件
1. 点击顶部 "📤 上传文件" 按钮
2. 在弹出的文件选择对话框中，选择要上传的文件
3. 可以按住 `Ctrl` 键选择多个文件
4. 点击 "打开" 完成上传
5. 文件会被复制到程序的同步文件夹中

### 2. 文件分类
左侧边栏提供文件分类筛选：
- **全部**：显示所有文件
- **文档**：.txt, .md, .doc, .pdf 等
- **代码**：.py, .js, .html, .css 等
- **图片**：.jpg, .png, .gif 等
- **压缩包**：.zip, .rar, .7z 等
- **其他**：其他类型文件

点击分类名称即可筛选显示。

### 3. 文件搜索
1. 在顶部右侧搜索框输入关键词
2. 支持文件名模糊搜索
3. 支持文件类型搜索（如输入 `.py`）
4. 输入后自动过滤显示结果

### 4. 文件预览
1. 在文件列表中点击选中文件
2. 右侧面板会显示文件详情
3. 如果是文本/代码文件，会自动显示内容预览
4. 双击文件可以用系统默认程序打开

### 5. 重命名文件
1. 选中要重命名的文件
2. 点击顶部 "✏️ 重命名" 按钮
3. 输入新文件名
4. 点击 "确定" 完成

### 6. 删除文件
1. 选中要删除的文件
2. 点击顶部 "🗑️ 删除" 按钮
3. 确认删除操作
4. **注意：删除操作不可恢复！**

### 7. 推送到GitHub
1. 确保已配置并连接GitHub
2. 点击左侧 "⬆️ 推送到云端" 按钮
3. 确认推送操作
4. 等待推送完成
5. 推送成功后，文件同步状态会变为 "已同步"

### 8. 从GitHub拉取
1. 确保已配置并连接GitHub
2. 点击左侧 "⬇️ 拉取到本地" 按钮
3. 程序会下载GitHub仓库中的所有文件
4. 拉取完成后自动刷新文件列表

### 9. 多端同步使用
在其他设备上使用相同配置：

1. 安装Python和PyGithub
2. 复制 `file_manager.py` 到新设备
3. 运行程序
4. 使用相同的GitHub令牌和仓库名称配置
5. 点击 "拉取到本地" 获取云端文件

---

## 常见问题解答

### Q1：提示"PyGitHub库未安装"
**解决方法：**
```bash
pip install PyGithub
```
如果提示权限错误，尝试：
```bash
pip install PyGithub --user
```

### Q2：GitHub连接失败，提示"令牌无效"
**可能原因：**
1. 令牌复制不完整
2. 令牌已过期
3. 令牌权限不足

**解决方法：**
1. 重新检查令牌是否完整
2. 重新创建令牌，确保勾选 `repo` 权限
3. 使用新令牌重新配置

### Q3：提示"仓库不存在或无访问权限"
**可能原因：**
1. 仓库名称格式错误
2. 仓库确实不存在
3. 令牌没有该仓库的访问权限

**解决方法：**
1. 检查仓库名称格式：`用户名/仓库名`
2. 确认仓库已在GitHub上创建
3. 如果是私有仓库，确保令牌有权限访问

### Q4：推送文件失败
**可能原因：**
1. 文件超过100MB（GitHub限制）
2. 网络连接问题
3. 令牌权限不足

**解决方法：**
1. 检查文件大小，GitHub单文件限制100MB
2. 检查网络连接
3. 确认令牌有 `repo` 权限

### Q5：程序运行后没有界面显示
**可能原因：**
1. Python版本不兼容
2. Tkinter库缺失

**解决方法：**
1. 确认Python版本 >= 3.8
2. Windows通常自带Tkinter，如果缺失：
   ```bash
   # Windows: 重新安装Python，勾选所有组件
   # Linux (Ubuntu/Debian):
   sudo apt-get install python3-tk
   # Linux (Fedora):
   sudo dnf install python3-tkinter
   ```

### Q6：文件存储在哪里？
**答案：**
- 本地文件存储在：`用户主目录/.file_manager/sync_files/`
- 配置文件存储在：`用户主目录/.file_manager/config.json`
- Windows路径示例：`C:\Users\您的用户名\.file_manager\sync_files\`

### Q7：如何查看GitHub上的文件？
**方法：**
1. 登录 GitHub.com
2. 进入您的仓库
3. 可以看到所有已推送的文件
4. 可以直接在网页上查看或下载

### Q8：可以在手机上查看文件吗？
**答案：**
可以！两种方式：
1. **浏览器方式**：手机浏览器访问 github.com，登录后进入仓库查看
2. **GitHub官方App**：下载GitHub手机App，登录后查看仓库

### Q9：配置文件中的令牌安全吗？
**答案：**
- 程序使用Base64编码存储令牌
- 这是一种简单编码，不是加密
- 请勿在公共电脑上保存令牌
- 建议定期更换令牌

### Q10：如何完全卸载/清理程序？
**步骤：**
1. 删除 `file_manager.py` 文件
2. 删除配置文件夹：
   - Windows：删除 `C:\Users\您的用户名\.file_manager\` 文件夹
   - Mac/Linux：删除 `~/.file_manager/` 文件夹
3. 如需卸载PyGithub：
   ```bash
   pip uninstall PyGithub
   ```

---

## 技术支持

如果遇到其他问题：
1. 检查Python版本：`python --version`
2. 检查PyGithub版本：`pip show PyGithub`
3. 尝试重新安装依赖：`pip install --upgrade PyGithub`

---

## 附录：快捷键说明

| 操作 | 快捷键 |
|------|--------|
| 刷新文件列表 | 双击文件列表空白处 |
| 打开文件 | 双击文件 |
| 搜索 | 在搜索框输入后自动搜索 |

---

## 更新日志

### v1.0.0 (2024)
- 首次发布
- 支持本地文件管理
- 支持GitHub云端同步
- 支持多端查看
- 支持文件分类和搜索
- 支持文本文件预览

---

**祝您使用愉快！**
