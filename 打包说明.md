# GitHub文件管理器 - 打包说明

## 目录结构

```
wenjian1/
├── file_manager.py      # 主程序文件
├── requirements.txt     # 依赖列表
├── build.spec          # PyInstaller配置
├── build.bat           # Windows打包脚本
├── build.sh            # Linux/Mac打包脚本
├── 使用教程.md          # 使用教程
└── 打包说明.md          # 本文件
```

## 快速打包（推荐）

### Windows系统

1. **双击运行 `build.bat`**
   - 脚本会自动安装依赖并打包
   - 打包完成后，可执行文件位于 `dist\GitHub文件管理器.exe`

2. **手动打包**
   ```bash
   # 安装依赖
   pip install -r requirements.txt
   
   # 打包程序
   pyinstaller --clean --noconfirm build.spec
   ```

### Linux/Mac系统

```bash
# 添加执行权限
chmod +x build.sh

# 运行打包脚本
./build.sh
```

## 详细打包步骤

### 1. 安装Python

**Windows:**
- 下载：https://www.python.org/downloads/
- 安装时勾选 "Add Python to PATH"

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3 python3-pip
```

**Mac:**
```bash
brew install python3
```

### 2. 安装依赖

```bash
pip install -r requirements.txt
```

国内用户可使用镜像加速：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 3. 打包程序

**方法一：使用打包脚本（推荐）**
- Windows: 双击 `build.bat`
- Linux/Mac: 运行 `./build.sh`

**方法二：使用PyInstaller命令**
```bash
# 打包为单个可执行文件
pyinstaller --onefile --windowed --name "GitHub文件管理器" file_manager.py

# 或使用配置文件
pyinstaller build.spec
```

### 4. 查找打包结果

打包完成后，可执行文件位于：
- Windows: `dist\GitHub文件管理器.exe`
- Linux/Mac: `dist/GitHub文件管理器`

## PyInstaller参数说明

| 参数 | 说明 |
|------|------|
| `--onefile` | 打包为单个可执行文件 |
| `--windowed` | 不显示控制台窗口 |
| `--name` | 指定输出文件名 |
| `--icon` | 指定程序图标 |
| `--clean` | 清理临时文件 |
| `--noconfirm` | 覆盖输出目录不询问 |

## 添加程序图标

1. 准备一个 `.ico` 格式的图标文件（Windows）
2. 修改 `build.spec` 文件：
   ```python
   exe = EXE(
       ...
       icon='icon.ico',  # 添加图标路径
       ...
   )
   ```

3. 重新运行打包脚本

## 常见问题

### Q1: 打包后程序无法运行

**解决方法：**
1. 检查是否安装了所有依赖
2. 尝试以管理员身份运行
3. 检查杀毒软件是否拦截

### Q2: 打包后文件过大

**解决方法：**
1. 使用虚拟环境减少不必要的包
2. 在 `build.spec` 的 `excludes` 中排除不需要的模块

### Q3: 打包后缺少模块

**解决方法：**
在 `build.spec` 的 `hiddenimports` 中添加缺失的模块：
```python
hiddenimports=[
    '缺失的模块名',
],
```

### Q4: Mac系统提示"无法验证开发者"

**解决方法：**
```bash
# 允许运行任何来源的应用
sudo spctl --master-disable

# 或移除隔离属性
xattr -cr dist/GitHub文件管理器.app
```

### Q5: Linux系统无法运行

**解决方法：**
```bash
# 添加执行权限
chmod +x dist/GitHub文件管理器

# 安装Tkinter依赖
sudo apt install python3-tk
```

## 分发说明

打包完成后，您可以将可执行文件分发给其他用户：

1. **直接分发**
   - 将 `dist` 目录中的可执行文件发送给用户
   - 用户无需安装Python即可运行

2. **创建安装包（可选）**
   - Windows: 使用 Inno Setup 或 NSIS 创建安装程序
   - Mac: 创建 .dmg 镜像文件
   - Linux: 创建 .deb 或 .rpm 包

## 更新程序

如需更新程序：

1. 修改 `file_manager.py` 源代码
2. 重新运行打包脚本
3. 新的可执行文件会覆盖旧的

## 版本信息

- 程序版本：2.0.0
- Python要求：3.8+
- 打包工具：PyInstaller 5.0+

---

如有问题，请参考 `使用教程.md` 或联系开发者。
